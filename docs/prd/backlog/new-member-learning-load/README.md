# New-Member Learning Load

## Status

- Tracker item: T-002 in `docs/prd/future-work.md`.
- Lifecycle folder: `backlog`.
- Artifact state: brief only; create `prd.md` and `test-spec.md` before implementation if this becomes active work.

## Why

신규 유입 사용자가 기존 누적 표현을 한 번에 마주치면 첫 학습 세션을 시작하기 어렵습니다.

## First Slice Direction

첫 학습 세션에서 노출할 카드 수를 제한하거나 단계적으로 해금하는 최소 정책을 정의합니다.

## Initial Verification Shape

- 큐 생성 로직 테스트
- `/memorize` live route smoke check
- 기존 사용자 due queue가 의도치 않게 줄어들지 않는지 확인

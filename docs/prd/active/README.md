# Active PRD Work

현재 구현 중인 PRD 작업을 둡니다.

- 한 번에 가능하면 1개 작업만 둡니다.
- `docs/prd/future-work.md`에서 해당 항목도 `Active`로 이동해야 합니다.
- 구현 브랜치/PR이 열린 작업은 `backlog/`에 남기지 말고 이 폴더로 이동합니다.
- 작업이 끝나면 해당 feature 폴더를 `docs/prd/complete/`로 이동하고 `future-work.md`의 `Complete`에 검증 증거를 남깁니다.
